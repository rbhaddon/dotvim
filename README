# Create a VIM Workstation Anywhere
# =================================

# Preliminary Git stuff if not done already
git config --global user.name "Firt Last"
git config --global user.email email@address.com
git config --global push.default "matching"
git config --global color.status auto
git config --global color.branch auto
 
# Now onto vim stuff
# ------------------

# Link .vimrc and .gvimrc to ~/
ln -s ~/.vim/vimrc ~/.vimrc
ln -s ~/.vim/gvimrc ~/.gvimrc

# The README from wnodom:
# Welcome to Bill Odom's Vim environment. Here's how to use it:
#
# - Copy (or clone) this directory to ~/.vim (or the equivalent for your OS).
#
# - Create the following symbolic links:
#
#     ln -s ~/.vim/.vimrc  ~/.vimrc
#     ln -s ~/.vim/.gvimrc ~/.gvimrc

# Updating submodules
# -------------------
# All or most of my pathogen bundles are git submodules.
# To add a new submodule:
git submodule add <repo> <path>
# To update them all:
git submodule sync
# -or-
git submodule init
git submodule update
# which is better?
